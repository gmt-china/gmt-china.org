dist: xenial
language: generic

cache:
  directories:
    - .htmltest

install:
  - curl -L https://raw.githubusercontent.com/seisman/install/master/install-hugo.sh | bash
  - curl -L https://raw.githubusercontent.com/seisman/install/master/install-htmltest.sh | bash

script:
  - ./hugo
  - ./htmltest

deploy:
  provider: pages
  skip_cleanup: true
  github_token: ${GH_TOKEN}
  local_dir: public
